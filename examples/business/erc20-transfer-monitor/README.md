# ERC20 Transfer Monitor

This example watches a single ERC20 contract and stores `Transfer` events from block receipts/logs.

Default contract: USDC on Ethereum mainnet.

## Queries

- `GetContractSummaryQuery` — contract totals and counters
- `GetRecentTransfersQuery` — recent transfer list
