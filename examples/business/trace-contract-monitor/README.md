# Trace Contract Monitor

This example watches block traces for one target contract and stores recent traces useful for debugging integrations.

Default contract: WETH on Ethereum mainnet.

## Important

This example requires trace-capable RPC support. Keep `TRACES_ENABLED=true` and use a provider that supports trace/debug RPC methods.
